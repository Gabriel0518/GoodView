# 广告转化看板 v2 —— 分期实现计划

> 配套《产品设计-v2.md》。三期落地：P1 数据地基 → P2 展示层重建 → P3 智能层。
> 说明：v2 相比 v1 **数据层也要动**（加 adset、事件 DB 化、分组、永久保留），展示层**完全重建**。

## 全局注意

- **数据层不停机**：P1 用 `ALTER TABLE` + 回补，不清库；v1 页面在 P2 上线前继续能用旧口径。
- **展示层灰度**：P2 新看板与 v1 三 tab 并存一段，稳定后再下 v1（`app/components/{Overview,FunnelView,DailyReport}.tsx` + `compute.ts` 退役）。
- **秘钥/部署**：沿用现有 `.env` + Railway（web + cron + Postgres）。新增 `KIMI_API_KEY`（P3）。

---

## P1 数据地基（可独立上线，先修成本失真）

### P1.1 campaign_daily 加 adset 维度
- **schema**：`campaign_daily` 加列 `adset_id text`、`adset_name text`；主键改为 `(date, account_id, campaign_id, adset_id)`。
- **迁移**：`ALTER TABLE campaign_daily ADD COLUMN ...`；旧行 adset 填占位（如 `'_'`）或直接重建后回补。建议**重建 + 回补一年**（数据量可控）。
- **拉取**：`fetch-snapshot.mjs` 的 `dim` 加 `adset_id, adset_name`（XMP 支持，见 `xmp-campaign.mjs`）；`CAMPAIGN_COLS` 增两列；transform 增 adset 字段。
- **影响**：行数增多（每 campaign 多个 adset）。**决定：adset 也回补全年**——预估百万行级、拉取较久（分段 + QPM 限），Postgres 可承受；一次性回补后每天增量。
- 文件：`db/schema.sql`、`fetch-snapshot.mjs`、`db/migrate.mjs`（可选加迁移语句）。

### P1.2 事件定义 DB 化
- **目标**：漏斗事件从 `funnel-events.mjs`（代码）迁到 DB，拉取脚本读 DB。
- **schema**：扩展 `funnel_stage_meta` 加 `enabled bool default true`、`source_split bool default true`、`indicator text default 'event_users'`（`filters/label/ord/event_name` 已有）。
- **播种**：一次性把 `funnel-events.mjs` 的 47 条写入 DB（`db/migrate.mjs` 已在做，补全新列）。
- **拉取改造**：`fetch-funnel.mjs` 从 `SELECT * FROM funnel_stage_meta WHERE enabled` 读事件定义，不再 import `funnel-events.mjs`。
- 文件：`db/schema.sql`、`db/migrate.mjs`、`fetch-funnel.mjs`；保留 `funnel-events.mjs` 仅作初始种子。

### P1.3 分组功能（P0，修 PWA 成本失真）
- **schema**：新表
  ```sql
  CREATE TABLE ad_groups (
    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    name text NOT NULL,
    members jsonb NOT NULL,  -- [{type:'account'|'campaign', id:'...'}]
    is_app_group bool DEFAULT false,  -- 标记"某 App 的花费集合"
    created_at timestamptz DEFAULT now()
  );
  ```
- **解析函数**（`app/lib/groups.ts`）：组 → 去重 `campaign_id` 集合（account 展开为其下全部 campaign；campaign 即自身；重叠只算一次）。查询层 `WHERE campaign_id IN (集合)` 求和 → 天然去重、不叠加。
- **初始化 PWA 组（手动指定）**：**决定：由你手动圈定**哪些账户/系列属于 PWA。做法——P1.3 先建**组管理页/接口**，列出所有账户+其花费+App 线索，你在里面勾选组成 "PWA" 组；默认看板用该组 → 成本只按 PWA 花费算。
- **管理入口**：P1 做组 CRUD API（`/api/groups`）+ 最小组管理 UI（列账户/系列供勾选）。
- 文件：`db/schema.sql`、`app/lib/groups.ts`、`app/api/groups/route.ts`、`app/admin/groups/page.tsx`。

### P1.4 保留 + 回补
- **永不 prune**（已是现状，确认不加清理）。
- **回补一年**：`pull:backfill` = 365（已改），`fetch-snapshot` 的 `dateChunks` 拆 5×≤90 天（已实现）。
- **BytePlus 漏斗 365 天**：先验证 `fetch-funnel.mjs 365` 是否报错；若报错，给 BytePlus 拉取也加分段（按日期窗口切，`funnel_daily` 按日期 delete+insert 天然可合并）。
- **去 v1 展示上限**：`app/lib/db-queries.ts` 的 `WINDOW_DAYS` 改为**参数化/视图驱动**（P2 查询引擎接管；P1 可先放宽到 400）。

**P1 产出**：库有一年数据、adset 维度、事件 DB 化、PWA 分组修成本。工作量中，风险点＝adset 回补数据量 + BytePlus 365 分段。

---

## P2 展示层重建（核心，工作量最大）

### P2.1 服务端查询引擎
- **端点**：`POST /api/query`
  ```
  { measure, dims:[x?,y?,z?](≤3), filters:{dateRange,granularity,groups?,accounts?,campaigns?,adsets?,sources?,stages?}, }
  → { rows:[{dimVals..., value}], meta:{aggType, note} }
  ```
- **SQL builder**（`app/lib/query-engine.ts`）：
  - 按 measure 所属**立方体**路由到 `campaign_daily` 或 `funnel_daily`；跨立方（单位成本）在 日期+渠道↔来源 join。
  - **三类聚合规则**（硬编码防错）：可加→SUM；比值→SUM(分子)/SUM(分母)；人数→SUM 日UV（标注"人次"）。
  - **粒度 rollup**：日/月/年 用 `date_trunc`；年视图 = 今日往前365天，钳制 `max(start, MIN(date))`。
  - **分组解析**：调 `groups.ts` 展开为 campaign_id 集合。
- 文件：`app/lib/query-engine.ts`、`app/api/query/route.ts`。

### P2.2 度量目录 + 兼容矩阵
- `app/lib/metrics.ts`：每个度量 = `{ key, label, group, cube, allowedDims[], aggType, formula }`。前端据此**灰掉不兼容维度**。
- 度量：花费/曝光/点击/CPM/CTR/CPC（花费）、人数（漏斗，配 stage 参数）、转化率（A→B）、单位成本（跨）。

### P2.3 多看板 + 持久化
- **schema**：
  ```sql
  CREATE TABLE dashboards (id..., name text, board_filters jsonb, is_template bool DEFAULT false, created_at, updated_at);
  CREATE TABLE cards (id..., dashboard_id bigint REFERENCES dashboards, title text, config jsonb, layout jsonb, ord int);
  ```
  - `board_filters`：`{app, dateRange, granularity, groupId?/accounts?/campaigns?, sources?}`
  - card `config`：`{measure, stageParam?, cvrFrom?, cvrTo?, dims{x?,y?,z?}, viz, cardFilters?}`
  - card `layout`：`{x,y,w,h}`（栅格）
- **CRUD API**：`/api/dashboards`（增删改查+复制+存模板）、`/api/dashboards/[id]/cards`。
- 文件：`db/schema.sql`、`app/api/dashboards/**`。

### P2.4 引导式卡片搭建器（前端）
- 组件：`CardBuilder`（选度量→兼容维度→图表→卡片筛选→预览→存）、`MeasurePicker`、`DimPicker`（灰掉+解释）、`VizPicker`。
- 走 `/api/query` 实时预览。
- 文件：`app/components/builder/*`。

### P2.5 看板页 + 拖拽栅格
- `react-grid-layout`（新增依赖）做拖拽/调大小；卡片位置存 `cards.layout`。
- 看板级筛选条（数据源选组/账户/系列、日期、粒度）→ 影响所有卡片。
- 文件：`app/components/board/*`、`app/dashboard/[id]/page.tsx`、`app/page.tsx`（改为看板列表）。

### P2.6 图表组件
- `number/line/bar/table/funnel` 五种，读 `/api/query` 结果渲染。复用现有 `charts.tsx` 的 SVG 折线/柱，新增透视表 + 漏斗。
- 文件：`app/components/viz/*`。

### P2.7 默认看板 + 复制/模板 + 退役 v1
- 播种默认看板：近30日 PWA 组 全渠道花费 + 触达 走势。
- 复制看板/存模板（API 已含）。
- 下线 v1：删 `Overview/FunnelView/DailyReport.tsx` + `compute.ts`（或保留只读）。

**P2 产出**：完整自助搭建器。工作量大，风险＝查询引擎正确性（三类聚合）+ 跨立方 join。

---

## P3 智能层

### P3.1 AI 解读（Kimi）
- **端点**：`POST /api/ai/interpret { dashboardId }` → 读该看板所有卡片的聚合结果 → 拼 prompt → Kimi（OpenAI 兼容）→ 返回洞察文本。
- 展示在看板**数据下方文字区**，**按需点击**触发。
- 配置：`KIMI_API_KEY`/`KIMI_MODEL`/`KIMI_BASE_URL`（.env 已备，model/base 待确认）。用 `openai` SDK 指向 Moonshot base。
- 文件：`app/lib/kimi.ts`、`app/api/ai/interpret/route.ts`、看板页文字区组件。

### P3.2 AI 推荐
- `POST /api/ai/suggest { dashboardId }` → 读看板上下文+数据显著变化 → Kimi 返回**建议卡片**(结构化, measure+dims) → 前端一键添加。

### P3.3 事件配置页面
- UI CRUD `funnel_stage_meta`（加/改/停用事件、task_id 过滤、source_split、漏斗顺序）。
- 改完下次 cron 拉取即生效。
- 文件：`app/admin/events/page.tsx`、`app/api/events/route.ts`。

**P3 产出**：AI 解读+推荐、事件自助配置。

---

## 顺序与依赖

```
P1.3 分组(修成本) ─┐
P1.1 adset ────────┤
P1.2 事件DB化 ─────┼─→ P2.1 查询引擎 ─→ P2.2~2.7 搭建器/看板 ─→ P3 AI/配置页
P1.4 保留/回补 ────┘
```
- P1 各项相对独立，可并行；P1.3（分组修成本）价值最高、最先。
- P2.1 查询引擎是 P2 的地基，先做。
- P3 依赖 P2 的看板/卡片数据结构。

## 风险清单

| 风险 | 缓解 |
|---|---|
| adset 回补数据量大 | 先评估行数；必要时 adset 仅存近 N 月 |
| BytePlus 365 天拉取报错 | 给漏斗拉取加分段 |
| 查询引擎三类聚合算错 | 单元对账（vs 现有 v1 数值）+ 严格度量目录 |
| 跨立方 join 口径 | 只在 日期+渠道↔来源 join，UI 明示 |
| 拖拽栅格工作量 | 用 react-grid-layout，不自研 |
| 人数"人次vs真去重" | MVP 标注人次，真去重留接口 |

## 建议节奏

- **先做 P1.3 + P1.1**（修成本失真 + adset），能立刻让现有 v1 的成本数变准（把默认口径切到 PWA 组）。
- 再推进 P2 查询引擎与搭建器。
- P3 最后。
