# v2 开发 Loop —— 开发·验证·优化·再开发

> 每个任务切片都跑这个闭环，直到达退出条件才算完成。目的：深度开发 + 持续优化，不留隐性 bug 和技术债。配套《DEV-AGENTS.md》《实现计划-v2.md》。

## Loop 总图

```
        ┌─────────────────────────────────────────────┐
        │                                             │
   ①开发 ──→ ②验证 ──→ ③提优化意见 ──→ ④采纳并再开发 ─┘
   (Dev)     (Verify)   (Review/Audit)   (Iterate)
        │                                             ▲
        └──── 达退出条件 → 该切片完成，进下一切片 ──────┘

   最多 5 轮/切片；无 P0/P1、优化建议收敛即退出。
```

## ① 开发 Dev

- 输入：任务切片（来自任务看板，范围明确、在文件归属权内）。
- 做：实现最小可验证的一片（不贪大），遵循现有代码风格。
- 输出：改动 + 一句"改了什么、为什么"。
- 硬性：不使用 `ignoreBuildErrors`/`@ts-ignore`/`eslint-disable` 掩盖问题（见全局 CLAUDE.md 第13条）；遇错找根因。

## ② 验证 Verify（不通过不进下一步）

按切片类型选用，**能自动验证就不靠人眼**：

| 切片类型 | 验证手段 |
|---|---|
| 类型/编译 | `npx tsc --noEmit`（只应剩已知的、非本次引入的错） |
| 脚本(.mjs) | `node --check`；纯函数逻辑写内联自测 |
| 数据层/口径 | `node reconcile.mjs`（库 vs 旧口径对账）、`node audit-*.mjs`；**新引擎数值必须和 v1 对得上**（可加/比值/去重三类分别验） |
| 后端 API | 直接 curl/fetch 端点，核对返回形状与数值 |
| 前端可见改动 | `preview_start` + `preview_eval/inspect/screenshot`，读真实卡片值、逐场景走查（选度量→维度→图表、分组、日/月/年、边界空数据） |
| 跨系统一致性 | 关键指标端到端核对（如 PWA 组成本 = 组内系列花费 ÷ 对应来源人数） |

- 输出：验证记录（跑了什么、结果、有无偏差）。发现问题 → 回 ①。

## ③ 提优化意见 Review / Audit

由 **Verify Agent**（或 `/code-review`、`/simplify` 技能）审当前切片，产出**排序的**建议，分四维：

1. **正确性**：口径错、边界（空数据/除零/时区/去重）、并发/连接（如 Railway proxy 连接）。
2. **简化/复用**：重复逻辑、可抽公共函数、能删的死代码。
3. **效率**：无谓全量、N+1 查询、前端重渲染、SQL 缺索引。
4. **一致性/契约**：偏离冻结契约、命名/风格不统一、缺口径标注（如"人次 vs 去重"）。

每条建议标：**严重度(P0/P1/P2/P3) + 具体位置 + 失败场景 + 修法**。P0=阻断、P1=功能/正确性缺陷、P2=视觉/体验、P3=优化。

## ④ 采纳并再开发 Iterate

- Lead 按严重度决定采纳哪些：**P0/P1 必修**；P2 视影响；P3 记入技术债清单（不当场做）。
- 采纳的回 ①，再走一轮。

## 退出条件（满足即该切片完成）

- ✅ 无 P0（阻断）
- ✅ 无 P1（功能/正确性缺陷）
- ✅ P2 ≤ 3 且不影响核心
- ✅ 或已到第 5 轮（剩余项记技术债，Lead 决策带病上还是拆新切片）

## 记录

- 每轮在**任务看板**更新状态；发现的 P3/技术债记入 `实现计划-v2.md` 的技术债区或独立 `TECH-DEBT.md`。
- 每个切片完成时，Lead 简述：交付了什么、验证通过项、遗留项。

## 这个项目的现成验证工具

- `npx tsc --noEmit` — 类型
- `preview_start` / `preview_eval` / `preview_inspect` / `preview_screenshot`（MCP）— 前端真实值
- `node reconcile.mjs` — 库 vs 旧 JSON 对账
- `node audit-ratios.mjs` / `node audit-unknown.mjs` — 漏斗/来源口径核对
- `node xmp-history-test.mjs` / 各 `byteplus-*probe.mjs` — 数据源探测
- `/code-review`、`/simplify`、`/verify` 技能 — 审查与验证
