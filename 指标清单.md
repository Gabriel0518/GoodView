# 广告转化看板 —— 指标清单（口径表）

> 交给开发对接用。分两部分：**XMP 侧**（广告平台数据）与**自有后台侧**（产品转化数据）。
> 看板中的「单位成本」类指标（每留咨成本、每注册成本、每首笔提现成本）由看板用 `XMP花费 ÷ 后台人数` 自行计算，**不需要开发单独提供**。

---

## 一、XMP 侧指标（Ad Report API）

来源：`POST https://xmp-open.mobvista.com/v2/media/account/report`
字段 ID 以 `POST /v1/media/report/fields`（`report_type=ad`）查询结果为准，下表为对应指标。

| 指标 | 建议字段 | 定义 | 取数方式 |
|------|---------|------|---------|
| Cost | `cost` | 广告花费 | 直接取 |
| Impressions | `impression` | 曝光数 | 直接取 |
| CPM | `cpm` | 千次曝光成本 = Cost / Impressions × 1000 | 直接取，或看板派生 |
| CTR | `ctr` | 点击率 = Clicks / Impressions | 直接取，或看板派生 |
| Outbound Clicks | `outbound_clicks` | 站外点击数（Meta 口径） | 直接取 |
| Cost per Outbound Clicks | `cost_per_outbound_click` | 单次站外点击成本 = Cost / Outbound Clicks | 直接取，或看板派生 |
| Link Clicks | `link_clicks` | 链接点击数（Meta 口径） | 直接取 |
| Registration complete | `registration_complete` / 转化事件 | 广告平台侧回传的「完成注册」转化数（Pixel/CAPI 或 MMP 归因） | 直接取 |

> 注意：
> - 各渠道（Meta/Google/TikTok）指标命名可能不同，务必先跑 `report/fields` 拿准确 `field` ID。
> - `Registration complete` 是**广告平台侧统计口径**，会与自有后台实际注册数有差异——两者都保留，正好可交叉核对归因质量。
> - 维度建议按 `date / channel / campaign / adset / ad` 拉取，便于和后台归因对齐。

---

## 二、自有后台侧指标

对应漏斗「触达 → 提现」环节。全部按 `日期 + 渠道 + campaign/ad + ad_type` 可筛选，并带归因字段（`click_id/campaign_id/ad_id`）。

| 指标 | 建议字段 | 定义 | 取数来源 |
|------|---------|------|---------|
| 主动入站留咨人数 | `active_inbound_leads` | 用户**直发**：主动发送预设短信到我方的去重人数（`ad_type=direct`） | `/api/sms/inbound` 去重手机号 |
| 被动入站留咨人数 | `passive_inbound_leads` | **留咨广告**：用户在广告表单留下电话的人数（`ad_type=lead`） | `/api/leads` |
| 触达人数 | `reached_users` | 成功下发并送达注册链接短信的去重人数 | `/api/sms/outbound` `status=delivered` 去重 |
| 真人点击链接数 | `real_link_clicks` | 去重真人点击注册链接数（需排除 bot/预取） | 链接点击打点 / `/api/registrations` 上游 |
| 注册人数 | `registrations` | 完成注册的用户数 | `/api/registrations` |
| 完成引导人数 | `onboarding_completed` | 完成新手引导流程的人数（对应 `pwa-funnel`） | `/api/onboarding/steps` |
| 首笔提现人数 ❓ | `first_withdrawal_users` | 完成**首次**提现（付款）的用户数 | `/api/withdrawals` `status=completed` 首笔 |
| 模拟通过 ❓ | `simulation_passed` | 完成/通过「模拟」环节的人数（**待定义**） | 待开发确认对应事件 |
| 首次通话 ❓ | `first_call_completed` | 完成首次通话的人数（**待定义**） | 待开发确认对应事件 |

---

## 三、待你/开发确认的 3 个口径

1. **「受笔提现人数」** —— 我按 **首笔提现人数**（首次完成提现的人数）理解，是否正确？还是指「所有提现笔数/人数」？
2. **「模拟通过」** —— 是哪个业务环节？（如模拟任务/试做通过）需要开发给出对应的打点事件名。
3. **「首次通话」** —— 指什么通话？（如首次接单通话/客服通话）同样需要对应事件名。

> 另外确认：以上「人数」类指标是否都要**去重**（按 user_id 或手机号），默认按去重人数统计。
