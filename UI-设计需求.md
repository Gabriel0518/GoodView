# UI 设计需求文档 —— v2 看板搭建器（浅色 / 欧美 SaaS 风）

> 设计方向：干净、克制、留白充足的现代 SaaS 界面。参考 Linear、Vercel Dashboard、Stripe、Amplitude、Retool、PostHog。
> 本文档定义**设计系统 + 逐页面 + 元素细节**，供前端实现（Tailwind）。替代 v1 的深色主题；现有 `/admin/groups` 等页面一并迁到浅色。

---

## 1. 设计原则

1. **浅色为底**：白/浅灰背景，内容优先，弱化装饰。
2. **留白充足**：宽松间距，信息不拥挤（SaaS 感的关键）。
3. **一个强调色**：靛蓝 accent 只用于主操作/选中/强调，其余用中性灰阶。
4. **层次靠边框+微阴影**：不靠重色块；卡片=白底+1px 边框+极浅阴影。
5. **数据克制**：图表用中性灰 + accent 点睛，语义色只在涨跌/告警。
6. **一致性**：所有页面复用同一套 token 与组件。

---

## 2. 设计系统（Design Tokens）

### 2.1 色彩

| 用途 | 名称 | Hex | 说明 |
|---|---|---|---|
| 页面背景 | bg-canvas | `#F7F8FA` | 最底层浅灰 |
| 卡片/面板 | bg-surface | `#FFFFFF` | 白 |
| 下沉区/输入底 | bg-subtle | `#F3F4F6` | 表头、hover、输入 |
| 边框 | border | `#E5E7EB` | 主边框 |
| 细分隔线 | border-hair | `#EEF0F2` | 表格行线 |
| 标题文字 | text-strong | `#111827` | gray-900 |
| 正文 | text-body | `#374151` | gray-700 |
| 次要 | text-muted | `#6B7280` | gray-500 |
| 占位/禁用 | text-faint | `#9CA3AF` | gray-400 |
| **强调色 accent** | accent | `#4F46E5` | 靛蓝 indigo-600（主按钮/选中/链接） |
| accent hover | accent-600 | `#4338CA` | 悬停加深 |
| accent 浅底 | accent-soft | `#EEF2FF` | 选中背景/标签底 indigo-50 |
| 成功 | success | `#16A34A` / 底 `#F0FDF4` | 涨、正向 |
| 危险 | danger | `#DC2626` / 底 `#FEF2F2` | 跌、删除 |
| 警告 | warning | `#D97706` / 底 `#FFFBEB` | 提示 |
| 信息 | info | `#2563EB` / 底 `#EFF6FF` | 中性提示 |

> 数据可视化调色板（多序列时按序取）：`#4F46E5`(accent) `#0EA5E9` `#10B981` `#F59E0B` `#EC4899` `#8B5CF6` `#64748B`。单序列默认 accent。

Tailwind：建议在 `tailwind.config.ts` 扩展 `colors` 为上述语义名（canvas/surface/subtle/border/accent…），全项目用语义类，不散用原始灰阶。

### 2.2 字体

- 字体族：`Inter, -apple-system, "Segoe UI", Roboto, sans-serif`；数字用 `font-variant-numeric: tabular-nums`（`.tnum`）。
- 尺寸/字重：
  - 页面标题 H1：20px / 24px，semibold(600)，`text-strong`
  - 区块标题：15px，medium(500)
  - 正文：14px，regular，`text-body`
  - 次要/说明：12–13px，`text-muted`
  - 大指标数字：28–32px，semibold，`.tnum`
- 行高：正文 1.5；标题 1.3。

### 2.3 间距 / 圆角 / 阴影

- 间距基准 4px；常用 8/12/16/24/32。卡片内边距 20–24px；卡片间距 16–20px。
- 圆角：卡片/面板 `12px`(rounded-xl)；按钮/输入/下拉 `8px`(rounded-lg)；标签/pill 全圆(`9999px`)。
- 阴影（极浅，SaaS 关键）：
  - 卡片 `shadow-sm`：`0 1px 2px rgba(16,24,40,.05)`
  - 浮层/下拉/模态 `shadow-md`：`0 4px 12px rgba(16,24,40,.08), 0 1px 3px rgba(16,24,40,.06)`
  - 悬浮态可加 `0 2px 8px rgba(16,24,40,.06)`
- 描边优先于阴影；hover 时轻微抬升（阴影 + `border` 略深）。

### 2.4 图标

- 线性图标（`lucide-react`，1.5px 描边），尺寸 16/18/20。颜色随文字（muted 为主，操作用 accent）。

### 2.5 动效

- 过渡 150–200ms `ease-out`；hover/展开/淡入。避免花哨；拖拽卡片时用轻微缩放+阴影加深。

---

## 3. 全局布局

**左侧栏 + 顶栏 + 内容区**（经典 SaaS 结构）：

```
┌───────────┬──────────────────────────────────────────────┐
│  Sidebar  │  Topbar（面包屑 / 页面标题 + 右侧操作/账户）    │
│  (240px)  ├──────────────────────────────────────────────┤
│           │                                              │
│  Logo     │            内容区（bg-canvas，居中最大宽度）    │
│  ─────    │                                              │
│  看板      │                                              │
│  广告分组  │                                              │
│  事件配置  │                                              │
│  ─────    │                                              │
│  设置      │                                              │
└───────────┴──────────────────────────────────────────────┘
```

- **Sidebar**：宽 240px，`bg-surface`，右侧 1px 边框。顶部 Logo/产品名(GoodView)。导航项：图标+文字，默认 `text-muted`，hover `bg-subtle`，选中 `bg-accent-soft` + `text-accent` + 左侧 2px accent 条。分组用细标题分隔。底部账户/设置。可折叠成 64px（仅图标）。
- **Topbar**：高 56–64px，`bg-surface` + 底部 1px 边框。左：面包屑或页面标题；右：页面级操作按钮、（无权限系统，可留数据更新时间"更新于 x · 每5分钟自动"）。
- **内容区**：`bg-canvas`，内边距 24–32px，最大宽度 1280–1440px 居中。
- 响应式：<1024px 侧栏收起为抽屉（汉堡按钮）。

---

## 4. 通用组件规范

### 4.1 按钮
| 类型 | 样式 |
|---|---|
| Primary | `bg-accent` 白字，hover `accent-600`，圆角 8，高 36，内边距 12/16，`shadow-sm` |
| Secondary | 白底 + `border` + `text-body`，hover `bg-subtle` |
| Ghost | 透明 + `text-muted`，hover `bg-subtle` |
| Danger | 白底 + `text-danger` + border-danger浅，hover 红浅底 |
| 图标按钮 | 32×32，圆角 8，hover `bg-subtle` |
- 禁用：50% 透明 + `cursor-not-allowed`。加载：内嵌 spinner + 文案"…中"。

### 4.2 输入 / 下拉 / 多选
- 输入框：白底 + `border`，圆角 8，高 36，focus 时 `border-accent` + 2px accent 光环（`ring-2 ring-accent/20`）。占位 `text-faint`。
- 下拉/多选（复用现有 `controls.tsx`，改浅色）：触发器=白底 pill/框，箭头 muted；浮层=白底 `shadow-md` 圆角 10；选项 hover `bg-subtle`，选中 `text-accent` + 勾。可搜索项顶部带搜索框。多选项前 checkbox（选中填 accent）。
- 日期选择：弹出式（快捷"最近7/14/30天/本月/本年"+ 起止），选中即生效。

### 4.3 卡片 / 面板
- 白底 + `border` + `shadow-sm` + 圆角 12。头部：标题(15px medium) + 副标题(12px muted) + 右侧操作区。内容内边距 20–24。

### 4.4 标签 / 徽章 (Badge/Pill)
- 圆角全，字号 12，内边距 2/8。中性=`bg-subtle`+`text-muted`；accent=`accent-soft`+`text-accent`；语义=对应浅底+深字（如"App组"=accent 徽章）。

### 4.5 表格
- 表头：`bg-subtle` 或白底 + 底部 1px 边框，12px medium `text-muted`，左对齐（数值右对齐）。行：白底，行线 `border-hair`，hover `bg-subtle`。数值列 `.tnum`。密度适中（行高 40–44）。

### 4.6 数字指标卡（KPI）
- 白底卡：小标题(12 muted) → 大数字(28–32 semibold .tnum) → 副信息(12，含环比徽章：↑绿/↓红 + 百分比)。

### 4.7 状态
- **加载**：骨架屏（浅灰 `bg-subtle` 块 + 微光扫过），不用整页 spinner。
- **空态**：居中插画/图标(线性、faint) + 一句说明 + 主操作按钮。
- **错误**：`bg-danger浅` + `text-danger` 条，含原因 + 重试。
- **Toast**：右上角，白底 `shadow-md` 圆角 10，左侧语义色竖条 + 图标，3–4s 自动消失。

### 4.8 图表（数据可视化）
- 折线：1.5–2px 线，accent 或调色板；网格线极浅 `#F1F3F5`；坐标轴 `text-faint` 11px；hover tooltip 白底 `shadow-md`。
- 柱状：圆角 4 顶；单色 accent，或按维度取调色板。
- 漏斗：横向条，从宽到窄，accent 渐深；每级标 人数 + 相邻转化率（转化率语义色）。
- 透视表：见 4.5，交叉表头加粗。
- 数字卡：见 4.6。
- 统一：空数据显示"—"或"None"（区分"0"与"无数据"）；比值/去重口径在角标或 tooltip 注明。

---

## 5. 页面逐个

### 页面 A：首页 / 看板列表（`/`）

**目的**：进入即看到所有看板，快速新建/进入/套模板。

**布局**：Topbar 标题"看板" + 右侧 `+ 新建看板`(primary)。内容区分两块：
1. **我的看板**：卡片网格（响应式 3–4 列）。每张看板卡：
   - 顶部小预览（可选：迷你趋势 sparkline 或 2×2 卡片缩略；MVP 可用纯色/图标占位）
   - 看板名（15px medium）
   - 元信息行（12 muted）：卡片数 · 数据源(组名) · 日期范围
   - hover：轻微抬升 + 右上角浮现 `⋯` 菜单（进入/复制/存为模板/删除）
   - 整卡点击进入看板。
2. **模板**（可折叠区）：横向卡片列表，"从模板新建"。内置默认模板：**投放概览**（近30日 PWA组 全渠道花费 + 触达 走势）。

**空态**：无看板时，居中"还没有看板" + `+ 新建看板` + "或从模板开始"。

**新建看板流**：点 `+ 新建看板` → 轻量弹窗：看板名 + 选数据源(组/账户/系列下拉) + 日期默认近30天 → 创建 → 进入空看板（引导加第一张卡）。

### 页面 B：看板详情页（`/dashboard/[id]`）

**目的**：固定筛选上下文下，自由排布卡片，按需 AI 解读。

**布局（自上而下）**：
1. **Topbar**：面包屑 `看板 / {名字}` + 右侧操作：`+ 卡片`(primary)、`编辑布局` 开关、`⋯`(复制/存模板/重命名/删除)。
2. **看板筛选条**（sticky，白底卡片条）：
   - 左：**数据源**（选组/账户/系列，pill 下拉）、**日期范围**（弹出）、**粒度**（分段控件 日/月/年）、**来源**（分段 合计/fb/tt/bff/公会/主动/被动/不明）。
   - 右：`更新于 x · 每5分钟自动`（muted）。
   - 说明：此条 = 所有卡片共享的"底"；改动实时刷新所有卡片。
3. **卡片栅格**（`react-grid-layout`）：
   - 12 列栅格，卡片可拖动、右下角拉伸调大小；空位提示"拖到此/加卡片"。
   - 每张卡片头：标题 + 右侧 `⋯`(编辑/复制/删除) + 拖拽手柄(编辑模式显现)。
   - 卡片体：按类型渲染（数字/折线/柱状/透视表/漏斗）。
   - `编辑布局` 关时锁定位置、隐藏手柄，纯查看。
4. **AI 解读区**（看板底部整宽面板）：
   - 折叠状态：一行 + `✨ 生成 AI 解读` 按钮（按需，不自动跑）。
   - 生成中：骨架 + "分析中…"。
   - 结果：白底卡，Markdown 洞察（趋势/异常/成本效率），下方"💡 推荐卡片"若干（每条=度量+维度描述 + `+ 添加` 一键建卡）。可 `重新生成`。

**空看板态**：栅格区居中"这个看板还没有卡片" + `+ 添加第一张卡片`（打开搭建器）。

### 页面 C：卡片搭建器（抽屉 Drawer 或居中 Modal）

**目的**：引导式搭建，防止无效组合，实时预览。

**布局**：右侧抽屉（宽 ~480px）或居中大弹窗，左配置右预览：
- **① 度量**：分组下拉/列表（花费/流量、人数、转化率、单位成本）。选中即锁定"立方体"。
- **② 拆分维度（X / 分组）**：只列**兼容维度**（据度量）；不兼容的**灰掉并带原因**（如"⊘ 系列—该度量无法按系列拆(漏斗无系列归因)"）。最多 3 维。
- **③ 图表类型**：分段/图标选择（数字/折线/柱状/透视表/漏斗），按 X 轴智能推荐默认。
- **④ 卡片级筛选**（可选）：日期(默认继承看板)、来源、阶段(若度量=人数需选阶段)等。
- **右侧实时预览**：走 `/api/query` 渲染当前配置结果；改配置即刷新（防抖）。
- 底部：`取消` / `保存到看板`（校验：度量+至少能出图）。

**交互细节**：
- 度量→维度→图表是**递进引导**（选完度量才亮起维度）。
- 灰掉项 hover 显 tooltip 解释，教育用户"为什么不能"。
- 人数度量默认提示"跨多日为人次(非去重)"角标。

### 页面 D：广告分组（`/admin/groups`）—— 现有页迁浅色

**目的**：把某 App 的账户/系列编组，隔离花费、修成本失真。

**布局**：Topbar 标题"广告分组" + `+ 新建组`。
- 说明条（info 浅底）：解释分组用途（成本只按组内算）。
- **组列表**：表格或卡片列表，每行：组名 + `App组`徽章 + 成员数(账户 x/系列 y) + 组内花费 + `编辑`/`删除`。
- **编辑器**（弹窗/抽屉）：组名输入 + "标记为 App 组"开关 + 两个可搜索多选（广告账户 hint=花费；广告系列 hint=账户名/花费）+ 已选成员 chips（可删，带类型徽章）+ "已选 N 项 · 组内花费 $X" + 保存/取消。
- 空态、候选未加载(提示先拉数)、错误 —— 按 §4.7。

### 页面 E：事件配置（`/admin/events`）—— P3

**目的**：页面增改 BytePlus 打点事件（漏斗阶段），改完下次拉取生效。

**布局**：Topbar "事件配置" + `+ 新增事件`。
- **事件表**：列 = 顺序(可拖排序) · 显示名 · event_name · 过滤(task_id 等) · 按source拆(开关) · 启用(开关) · 操作。
- **编辑器**：显示名、event_name、过滤条件（属性=值，可加多条）、indicator(去重人数/次数)、source_split、enabled、漏斗顺序。
- 说明：改动写 `funnel_stage_meta`，下次 cron 拉取按新定义取数。

---

## 6. 数据可视化细则

- **颜色语义**：涨=success、跌=danger、告警=warning；中性数据用灰阶+accent。
- **口径标注**：比值不可加、人数=人次、跨立方在 日期+渠道↔来源 —— 在卡片副标题或 tooltip 用 muted 小字标明。
- **空/无数据**：显示"None/—"，不显示误导性 0；缺数据日期不画（年视图从有数据首日起）。
- **数字格式**：货币 `$1,234.56`；大数 `1.2M/34.5k`（可配千分位）；百分比 1 位小数；`.tnum` 对齐。

---

## 7. 响应式

- ≥1280px：三栏内容、看板 3–4 列卡片网格、栅格 12 列。
- 1024–1280：卡片 2–3 列。
- <1024px：侧栏收抽屉；筛选条换行/收进"筛选"按钮；栅格降为单列堆叠（拖拽在小屏可禁用）。

## 8. 交互与无障碍

- 键盘可达（Tab 顺序、Enter 提交、Esc 关弹层）；focus 有可见光环。
- 颜色对比达 WCAG AA（正文 text-body 对白底 ≥ 4.5:1）。
- 所有操作有即时反馈（hover/active/loading/toast）。
- 破坏性操作（删看板/组/事件）二次确认。

---

## 附：与现状的迁移

- v1 现有深色组件（`ui.tsx`/`controls.tsx`/`charts.tsx`/各页）在 P2 重建时按本浅色系统重写；`tailwind.config.ts` 先落 §2 的语义色 token。
- 现有 `/admin/groups` 页从深色迁浅色（同一功能，换皮肤）。
- 图表复用现有 SVG 折线/柱基础，改配色 + 加透视表/漏斗。
