# 广告转化看板 —— 自有后台接口确认清单

> 交给开发确认。看板由需求方手动拉取更新，全部接口**只读**、返回 JSON、支持按 `日期 + 渠道 + campaign/ad + ad_type` 筛选。
> 广告平台侧（曝光/点击/花费/CPM/CTR/Outbound Clicks/Registration complete）走 XMP Open API，本清单只覆盖**自有后台**。

---

## ✅ 逐个确认清单（照此问开发）

### 接口 1：漏斗汇总 `GET /api/analytics/funnel`
按日期返回各阶段去重人数，看板首页漏斗图用。

**必须返回的字段（每个日期一行）：**
```json
{
  "date": "2026-07-01",
  "active_inbound_leads": 0,      // 主动入站留咨（直发）
  "passive_inbound_leads": 0,     // 被动入站留咨（留咨广告）
  "reached_users": 0,             // 触达人数
  "real_link_clicks": 0,          // 真人点击链接数
  "registrations": 0,             // 注册人数
  "onboarding_completed": 0,      // 完成引导人数
  "simulation_passed": 0,         // 模拟通过
  "first_call_completed": 0,      // 首次通话
  "first_withdrawal_users": 0,    // 首笔提现人数
  "withdrawal_amount": 0          // 提现付款金额
}
```
**要问：**
- [ ] 这些字段能否在**一个接口**里按天返回？
- [ ] 入参能否支持 `start_date / end_date / channel / ad_type / campaign_id / ad_id / geo`？
- [ ] 人数均按**去重**（user_id 或手机号）统计？
- [ ] `simulation_passed / first_call_completed` 在漏斗里的**先后顺序**是什么（放在哪两步之间）？

---

### 接口 2：带归因的转化聚合 `GET /api/analytics/attribution`
按广告维度聚合，用于和 XMP 花费 join，看板「渠道/广告明细表」用。

**入参：** 同接口 1 + `group_by=channel|campaign_id|ad_id|ad_type`
**必须返回（每个维度 key 一行）：** 归因字段 + 与接口 1 相同的全部指标
```json
{
  "channel": "meta", "campaign_id": "", "campaign_name": "",
  "ad_id": "", "ad_type": "lead",
  "registrations": 0, "onboarding_completed": 0,
  "simulation_passed": 0, "first_call_completed": 0,
  "first_withdrawal_users": 0, "withdrawal_amount": 0
  // ...其余同接口 1
}
```
**要问：**
- [ ] 每条转化记录是否落库了 `click_id / campaign_id / ad_id / channel / ad_type` 归因字段？（**这是花费对齐的命脉**）
- [ ] 若注册链接当前未携带广告参数，能否补齐？

---

### 接口 3：各阶段明细（下钻 / 核对，可分多个接口）
用于按维度下钻和与汇总数核对。全部支持分页、日期筛选、带归因字段、手机号脱敏。

| 接口 | 对应指标 | 要问是否已有 |
|------|---------|:--:|
| `GET /api/sms/inbound` | 主动入站留咨（直发） | [ ] |
| `GET /api/leads` | 被动入站留咨（留咨广告） | [ ] |
| `GET /api/sms/outbound` | 触达（送达状态 delivered） | [ ] |
| `GET /api/registrations` | 真人点击链接 / 注册 | [ ] |
| `GET /api/onboarding/steps` | 完成引导（`pwa-funnel` 分步） | [ ] |
| `GET /api/events/simulation` | 模拟通过明细 | [ ] |
| `GET /api/events/first-call` | 首次通话明细 | [ ] |
| `GET /api/withdrawals` | 首笔提现 / 提现金额 | [ ] |

---

### 接口 4：维度元数据 `GET /api/dimensions/campaigns`
campaign/ad 的 `id → name`、所属渠道、广告类型映射表（看板做下拉筛选和 join 用）。
- [ ] 是否有此映射接口？

---

## 通用约定（一并向开发确认）

- [ ] **分页**：列表接口支持 `page`（从 1）/ `page_size`（默认 200，最大 1000）。
- [ ] **日期与时区**：入参 `YYYY-MM-DD`；返回时间戳标注时区（建议与 XMP 账户时区/UTC 对齐）。
- [ ] **响应格式**：`{ "success": true, "data": {...} }`。
- [ ] **鉴权**：内部自用，固定 token 或 IP 白名单即可。
- [ ] `/guild/pwa-funnel` 页面背后的接口能否直接复用？（很可能已覆盖接口 1/3 的一部分）

---

## 备注：看板自行计算、无需开发提供的指标

- 每主动/被动留咨成本、每注册成本、每完成引导成本、**每首笔提现成本** = `XMP花费 ÷ 对应人数`
- 各阶段转化率 = 相邻两阶段人数相除
