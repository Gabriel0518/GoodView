# 广告转化看板 v3 —— 画布式工作台（设计 + 实现计划）

> 从 v2「侧边栏 + 路由页」转为 v3「单画布桌面 + 弹窗」。参考飞书仪表盘。
> **只重组外壳/交互，不动数据层与查询引擎/AI**（v2 后端全部复用）。

## 0. 已定决策

| 项 | 决定 |
|---|---|
| 导航范式 | 无「页面」概念：主界面 = 一张可缩放的桌面画布；导航/配置走浮动侧栏 + 弹窗 |
| 深链 | **弹窗 + 保留 URL 深链**：`/?board=<id>`、`/?modal=groups|events`（可分享/刷新直达） |
| 看板内布局 | **保持栅格**（react-grid-layout，沿用 v2 BoardGrid） |
| 桌面卡片 | **纯文字信息**（名称/卡片数/数据源/窗口），无迷你图预览 |
| 缩放 | 滚轮缩放 + 拖拽平移画布；看板卡片自由摆放（非栅格吸附） |
| 复用 | 查询引擎/度量目录/CRUD/AI/groups+events 逻辑/viz/CardBuilder 全部不变 |

## 1. 交互层次（飞书式）

```
桌面画布 /  (无限画布, 滚轮缩放, 拖拽平移)
 ├─ 看板卡片 × N   ← 自由摆放; 纯文字基本信息; 点击 → 打开看板弹窗(?board=id)
 ├─ 浮动侧栏(悬浮/可收起)  ← 新建看板 · 广告分组(弹窗) · 事件配置(弹窗)
 └─ 弹窗层(由 URL query 驱动)
      ├─ 看板弹窗 BoardModal(?board=id)  ← 内含 栅格数据卡片 + 看板筛选条 + AI 区
      │     └─ 点数据卡片 → CardBuilder 弹窗(编辑数据配置)
      ├─ 广告分组弹窗 GroupsModal(?modal=groups)
      └─ 事件配置弹窗 EventsModal(?modal=events)
```

## 2. 数据层（唯一改动）

- `dashboards` 加 `canvas jsonb NOT NULL DEFAULT '{}'`：`{x, y, w?, h?}` 画布坐标/尺寸。
- 迁移：`ALTER TABLE dashboards ADD COLUMN IF NOT EXISTS canvas jsonb NOT NULL DEFAULT '{}'`（幂等，加进 schema.sql）。
- 新建看板时给不重叠的初始坐标（按已有卡片错位）。

## 3. 前端架构

| 组件/文件 | 职责 |
|---|---|
| `app/page.tsx` | 画布页：读 URL query(board/modal) 决定叠加弹窗；渲染 Canvas |
| `app/components/canvas/Canvas.tsx` | 缩放/平移容器（react-zoom-pan-pinch）；渲染看板卡片（绝对定位） |
| `app/components/canvas/DesktopCard.tsx` | 桌面看板卡片（纯文字信息 + hover 菜单：打开/复制/存模板/重命名/删除）；拖动持久化 canvas 坐标 |
| `app/components/canvas/FloatingDock.tsx` | 浮动侧栏（悬浮竖条/FAB，可收起）：新建看板 · 分组 · 事件 |
| `app/components/board/BoardModal.tsx` | 看板弹窗：内嵌 BoardFiltersBar + BoardGrid + AiInsightPanel（现有）；关闭清 URL |
| `app/components/admin/GroupsModal.tsx` | 广告分组弹窗（抽 /admin/groups 逻辑） |
| `app/components/admin/EventsModal.tsx` | 事件配置弹窗（抽 /admin/events 逻辑） |
| `app/components/AppShell.tsx` | 去掉固定侧栏 → 全屏画布容器 + 浮动 dock；layout 去 padding |

**URL 深链**：用 query 参数（`?board=3`、`?modal=groups`）由 `app/page.tsx` 解析并叠加对应弹窗。`useSearchParams` + `router.push`（浅路由，不刷新画布）。旧 `/dashboard/[id]`、`/admin/*` 保留为兜底重定向到 `/?...`（或直接删，改后不再有入口）。

## 4. 复用（不变）

查询引擎、`/api/query`、度量目录、dashboards/cards CRUD、AI(interpret/suggest)、事件/分组 API、`BoardGrid`/`CardBuilder`/`BoardFilters`/`viz/*`/`groups.ts`、浅色 token/`ui.tsx`/`controls.tsx`/`charts.tsx`。

## 5. 分期实现

- **V3.1 画布基座**：`dashboards.canvas` 列 + 迁移；`Canvas`(zoom/pan) + `DesktopCard`；`app/page.tsx` 改画布；拖动/缩放 + 位置持久化；空态。依赖 `react-zoom-pan-pinch`。
- **V3.2 看板弹窗**：`BoardModal`（?board 深链）内嵌现有 BoardGrid + 筛选条 + AI；点数据卡片 → CardBuilder；新建看板流。
- **V3.3 浮动侧栏 + 配置弹窗**：`FloatingDock`；`GroupsModal`/`EventsModal`（抽现有页逻辑，?modal 深链）；退役固定侧栏 AppShell；旧 admin/dashboard 路由重定向或删除。
- **V3.4 打磨**：缩放边界/复位/（可选迷你地图）、深链刷新直达、响应式（小屏禁拖拽/降级）、空态/错误、code-review 收敛。

## 6. 依赖

- `react-zoom-pan-pinch`（画布缩放平移；轻量、成熟，避免自研）。

## 7. 待打磨/存疑

- 画布是否要"复位/适应窗口"按钮、迷你地图？（V3.4 视需要）
- 小屏（<1024）画布拖拽体验：降级为纵向列表？
- 旧路由：重定向 vs 删除（倾向重定向兜底深链）。

## 8. 技术债（评审记录）

- **moveCard 竞态（P3）**：拖动卡片是乐观更新 + 不 await 的 PUT；若在 PUT 在途时触发 load()（如菜单复制/删除后 refetch），可能读回旧坐标把卡片弹回。低频、下次拖动/刷新自愈。彻底修需：load() 保留在途乐观坐标，或按看板级 pending 标记合并。
- **配置弹窗关闭**：GroupsModal/EventsModal 去掉了 Esc 关闭（内层编辑器打开时 Esc 会误关丢草稿）；仅 X / 背景点击关闭。背景点击在内层 z-50 编辑器打开时被其遮挡，天然安全。
