# 广告转化看板 v2 —— 自助式看板搭建器（产品设计草案）

> 从"3 个固定 tab"升级为"多看板 + 引导式卡片搭建 + AI 解读"。本文档是讨论基线。

## 0. 已定决策（讨论中锁定）

| 项 | 决定 |
|---|---|
| 卡片搭建 | **引导式**：先选度量 → 系统只给能对齐的维度当 X/Y，灰掉不可选的并解释原因 |
| AI | **解读 + 推荐**，**按需点击触发**（不自动跑，省 token、可控） |
| 旧看板 | **完全重建**（不保留概览/漏斗/日报） |
| 布局 | **拖拽栅格**（卡片可拖动、调大小） |
| 数据层 | **要扩展**：加 adset 维度 + 事件定义改为 DB 可配置；拉取机制/5分钟 cron 不变 |
| 快捷度量优先级 | 前段(触达/安装/登录) + 后段(提现/任务) + 成本类；中段(引导/IG授权/mock) 次之 |
| AI 位置 | 看板数据**下方一个文字区**，搭建好后**点击启动**解读 |
| 查询粒度 | 日期 **日/月/年**；广告维度 **账户 account / 系列 campaign / 广告组 adset（三层）**；透视表**最多 3 维** |
| 首屏 | 一律走引导式搭建器；内置**默认看板**：近30日 全渠道花费 + 触达走势 |
| 看板管理 | 搭好后可**复制**、可存为**模板** |
| 分组 | 账户/系列**可混装编组**；解析成去重的叶子集合再算（不叠加，见 §10.2） |
| 成本口径修正 | 用分组把 PWA 的账户/系列圈出来，成本**只按 PWA 花费**算（修"全App花费算PWA成本"的失真）**P0** |
| 手动更新 | **不做**；刷新页面自动读库最新（cron 每5分钟拉取），无按钮 |
| 事件配置 | **页面可配置** BytePlus 打点事件（存 DB，取代 funnel-events.mjs 代码）|
| 多 App | 下个版本纳入（App 作为一级筛选维度）|
| 当前阶段 | **磨设计**，未进入实现 |

## 1. 定位

给这套广告转化数据做一个**轻量自助 BI**：用户自己建看板、拖度量和维度组合卡片、AI 帮读数。
解决现痛点：筛选条件每次重复拼、存不下来、卡片写死。

## 2. 对象模型

```
首页（看板列表）
 └─ 看板 Dashboard   ← 名字 + 固定筛选上下文（App / 日期范围 / 账户[] / 系列[] / 来源[]）
     ├─ 卡片 Card     ← 度量 + X轴 + Y轴/分组 + 卡片级筛选 + 图表类型 + 栅格位置
     ├─ 卡片 Card
     └─ AI 解读 / 推荐
```

- **看板级筛选** = 该看板所有卡片共享的"底"（进来即用，不用重拼）。
- **卡片级筛选** = 在底上再细化/覆盖。
- 存储：Postgres 新增 `dashboards`、`cards` 表，配置存 JSONB。

## 3. 引导式卡片搭建（核心）

流程：**选度量 → 系统给出兼容维度 → 选 X/Y 轴 + 图表 → 渲染**。
度量决定它属于哪个"立方体"，系统据此只放能对齐的维度，从源头防止无效组合。

### 度量目录（每个度量标注：立方体 / 可用维度 / 可用图表）

| 组 | 度量 | 立方体 | 可用维度(X/Y) |
|---|---|---|---|
| 花费/流量 | 花费、曝光、点击、CPM、CTR、CPC | 花费(campaign_daily) | 日期、渠道、账户、系列 |
| 人数 | 各阶段人数（触达/安装/登录/完成引导/IG授权/mock通过/首笔提现…） | 漏斗(funnel_daily) | 日期、来源、阶段 |
| 单位成本 | 单安装/单登录/单IG授权/单提现成本（渠道花费÷来源人数） | 跨立方体 | 日期、渠道↔来源（仅 fb/tt 有花费） |
| 转化率 | 阶段A→阶段B 转化率 | 漏斗 | 日期、来源 |

## 4. ⚠️ 双立方体约束（产品命门）

- **花费立方体**：日期 × 渠道 × 账户 × **系列**（无来源/阶段）
- **漏斗立方体**：日期 × **来源** × **阶段**（无系列）
- 两者**只在"日期 + 渠道↔来源近似"上能对齐**（fb↔facebook、tt↔tiktok；bff/AIguild/unknown 无花费，google 无来源）。
- **系列 × 漏斗阶段无法连接**（BytePlus 无 campaign_id，归因缺口）。
- 引导式搭建据此**只给能对齐的维度**，跨立方度量（单位成本）限制在 渠道↔来源 + 日期 粒度。

## 5. 可视化类型

| 类型 | 结构 |
|---|---|
| 数字卡 | 单度量，无分组 |
| 折线 | X=日期，series=一个维度 |
| 柱状 | X=一个维度，Y=度量 |
| 透视表 | X + Y 两维交叉 |
| 漏斗图 | 多阶段度量（特例） |

## 6. AI 解读 + 推荐

- **解读**：读整个看板的卡片数据 → 生成分析（趋势、异常、成本效率恶化的系列/来源、单提现成本变化）。
- **推荐**：读看板筛选上下文 + 数据显著变化 → 建议该加哪些卡、哪里有异常。
- 技术：**Kimi（Moonshot）**，OpenAI 兼容接口。key 走环境变量 `KIMI_API_KEY`（已存 .env），`KIMI_MODEL`/`KIMI_BASE_URL` 待确认（暂填 kimi-k2 / api.moonshot.cn）。输入=聚合数据+看板配置，输出=结构化洞察。

## 7. 架构影响（相对现状的大改）

- **服务端查询引擎**（新）：前端把透视配置（度量/维度/筛选/日期）发给 `POST /api/query`，后端按度量所属立方体拼 SQL `GROUP BY`（campaign_daily 或 funnel_daily，跨立方在 日期+渠道↔来源 join）返回聚合行。**替代**现在"塞全量、前端算"。
- **持久化**：dashboards/cards 表（JSONB 配置）。
- **LLM 集成**：AI 解读/推荐端点。
- 现有 compute.ts 客户端聚合逻辑大部分退役，改为查询引擎。

## 8. MVP 分期（建议）

- **P1 多看板骨架 + 看板级固定筛选 + 持久化** —— 直接解决"存不下、重复拼"。先用现有卡片逻辑塞进看板。
- **P2 服务端查询引擎 + 引导式卡片搭建** —— 度量目录 + 兼容维度 + 图表类型。核心。
- **P3 AI 解读 + 推荐**。

## 9. 待定问题

- 度量目录里哪些是你最常看的（决定先做哪些）？
- AI 推荐的触发方式：进看板自动跑，还是按需点？
- 看板布局：拖拽栅格 vs 简单堆叠（拖拽工作量大）？
- 是否保留手动/定时拉取不变（数据层不动，只改展示层）？

## 10. 数据源需求（本轮定稿）

### 10.1 无手动更新
- **不做手动刷新按钮**。cron 每 5 分钟拉取写库；前端刷新页面即读库最新（force-dynamic 已实现）。

### 10.2 广告账户/系列分组（P0，兼修成本失真）
- 用户把某几个**账户**或**系列**（可混装）编成命名组（如"PWA"）。
- **去重解析**：组 → 去重的叶子集合（账户展开为其下全部系列；具体系列即自身；重叠只算一次），`WHERE campaign_id IN (集合)` 求和。
  - 例：2 账户共 10 系列，只挑 6 → 算这 6 个系列，**不是** 2账户+6系列（不叠加）。
  - 只选账户不选系列 → 算该账户全部系列。
- **成本失真修复**：XMP 花费是全 App 的；把 PWA 对应账户/系列编组，成本只按该组算 → 单转化成本才准。默认看板用 PWA 组。
- 组可作**看板级固定上下文**（看板筛选里"数据源"直接选一个组）。

### 10.3 广告维度三层
- **账户 account > 系列 campaign > 广告组 adset**（Meta 层级）。
- 现只拉到 campaign；**需给 XMP 拉取加 `adset_id/adset_name` 维度**（campaign_daily 增列、行数增多）。

### 10.4 事件配置页面
- **页面可配置 BytePlus 打点事件**：加/改事件（event_name、显示名、task_id 等过滤、是否按 source 拆、漏斗顺序）。
- 事件定义从 `funnel-events.mjs`（代码）迁到 **DB 表**（`funnel_stage_meta` 已是雏形），拉取脚本读 DB 而非代码。

### 10.5 数据保留与展示窗口
- **数据库永久保留所有历史，永不删**（去掉 v1 的 180 天展示上限 `WINDOW_DAYS`）。
- **XMP 有长历史（≥1 年，实测到 2025-07 仍有数据），90 天只是单次请求上限** → 可分段回补一整年（5×≤90天，`dateChunks` 已支持）。`pull:backfill` = **365 天**。
  - BytePlus 漏斗侧 365 天一次拉取需验证；若报错则也分段。
- **展示窗口由视图/日期选择驱动**，并**钳制到有数据的范围**：
  - 年视图 = 今日往前 365 天；起点 = `max(今日-364, 最早有数据日)`。
  - 没数据的日期不展示，从有数据第一天起。
  - 日/月视图同理，按所选范围 + 数据可用性钳制。

## 11. ⚠️ 度量叠加 / 去重规则（数据正确性命门）

查询引擎按度量类型决定怎么聚合，**不能一律 SUM**：

| 类型 | 度量 | 跨日期/维度聚合方式 |
|---|---|---|
| **可加** | 花费、曝光、点击 | 直接 `SUM`（跨日期/账户/系列/渠道/来源/阶段都对） |
| **比值(不可加)** | CPM、CTR、CPC、转化率、单位成本 | **永不 SUM 比值本身**；从分量重算 `SUM(分子)/SUM(分母)` |
| **人数(UV/去重)** | 各阶段人数 | 见下 ⚠️ |

### 人数的去重陷阱（重点）
- `funnel_daily` 存的是**每日去重人数(UV)**。
- **跨日期求和 = 人次(累计)，不是"周期去重人数"** —— 同一用户多日出现会被重复计。
  - **一次性事件**(IG授权/首笔提现/安装成功，用户一生一次)：日求和 ≈ 真去重，可接受。
  - **重复性事件**(投广页曝光/Home曝光/登录页，用户可多日出现)：日求和会**高估**真去重人数。
- 真·周期去重需重新按周期查 BytePlus(granularity=整段)，当前预聚合模型给不出。
- **决策建议**：MVP 用"日去重之和(人次)"并**如实标注为「人次」**；个别必须真去重的指标，单独按周期查 BytePlus。
- **进阶（留接口，之后做）**：去重做成"可加筛选条件的事件"——每个事件可叠加属性过滤再计数。先留 DSL/接口，想清楚再实现。

## 12. 待确认（剩余）

1. ~~广告组层级~~ → **account/campaign/adset 三层**（已定，需加 adset 拉取）。
2. ~~手动更新~~ → **不做，刷新页面自动读库**（已定）。
3. ~~分组混装/去重~~ → **解析成去重叶子集合**，组可作看板级上下文（已定）。
4. ~~去重口径~~ → MVP 人次+标注，真去重留"可加筛选事件"接口（已定，接口先留）。
5. ~~年视图保留多久~~ → **库永久保留全部历史**；年视图=今日往前365天，钳制到有数据范围；**XMP 有长历史，90天仅单次请求上限，可分段回补一整年**（`pull:backfill`=365）（已定）。
6. ~~新事件谁加~~ → **页面可配置**（已定）。
7. ~~多 App~~ → 下版本；本版用分组隔离 PWA 花费即可（已定）。
